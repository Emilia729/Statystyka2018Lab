p<-1/2
q<-1/2
k<-24
n<-30
wynik<-factorial(n)/(factorial(n-k)*factorial(k))*p^k*q^(n-k)
wynik
exp<-runif(30)
ifelse(exp<=0.5, 0, 1)
exp2<-ifelse(exp<=0.5, 0, 1)
sum(ifelse(exp<=0.5, 0, 1))
vec=c()

function fun(n){
for(i in 1:n){
  x<-runif(n)
  y<-ifelse(x<=0.5, 0, 1)
  ile<-sum(y)
  vec<-vec(vec, ile)
}
  return(vec)
}
  
